<div class="add-category-product main">
<div class="button">
	<span><a href="/categories/new">Add Category</a></span>
</div>
<div class="spacer"></div>

<h5 class="tac nm subscript">Current Categories</h5>
    
      <% @categories.each do | category | %>
      <p class="category-list"><%= category.name %> - <%= link_to "Edit", edit_category_path(category) %></p>
      <% end %>
  </div>